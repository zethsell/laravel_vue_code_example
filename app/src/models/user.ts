export type UserRegister = {
    name: string
    email: string
    password: string
    password_confirmation: string
}

export type UserInformation = {
    id: number
    name: string
    email: string
}
